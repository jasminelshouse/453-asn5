#include <stdio.h>
#include <stdlib.h>



int stuff(int argc, char *argv[]) {
    if (argc < 3) {
        print_usage();
        return 1;
    }
    
/*Here we have to parse all the args, open the image file, 
read filesystem metadata*/
    
    return 0;
}
